ArcturusIDE\Development\ImplementationPlan\prompt_UI-Refinement.md
Please modify the model selection drop-down area to instead feature a local(pc icon) and cloud(cloud icon) to the left of the model selection which will act as a filter. 
Then for the drop down model list modify the list to use the same visual aesthetic as the main menu.
Id like all drop-down menus to universaly use the same visual aesthetic as the main menu.

Change where it says "uplink stable" to actually list the current model in use.